import React from 'react'
import { Navigate } from 'react-router-dom'

const Dashboard = () => {
  /*if(!isAuthenticated) {
    return (
      <Navigate to={"/login"} />
    )
  }*/
  return (
    <div>Dashboard</div>
  )
}

export default Dashboard